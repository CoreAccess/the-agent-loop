# Proposed Memory

Draft durable memory candidates live here until the user approves, edits, rejects, or asks to defer them.

Rules:

- Do not store secrets, credentials, API keys, tokens, or PII.
- Use one file per candidate unless a batch review is easier.
- Include source stamp, classification, rationale, risk, and proposed index hook.
- Do not move candidates to `.agent-loop/memory/active/` without approval.

