# Run Prompt

Use this prompt when starting the fresh isolated Codex CLI session:

```text
You are in a clean experiment project. Treat the current project folder as the entire world for this experiment. Do not inspect, open, search, or reference files outside this experiment project folder. Read AGENTS.md first, then TASK.md. Follow the experiment instructions exactly. Use Experiment 001's Goal Packet and atomic memories from input/ as your source of truth. Create the required scaffold and test reports under output/. Do not use prior knowledge of The Agent Loop. Do not browse or install dependencies.
```
