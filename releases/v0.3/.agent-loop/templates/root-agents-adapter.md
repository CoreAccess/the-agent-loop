# Root AGENTS.md Adapter Template

Use this only during onboarding. Root `AGENTS.md` is the only root file The Agent Loop creates or updates by default.

```markdown
# AGENTS.md

This project uses The Agent Loop. At session start, read and follow `.agent-loop/START.md`.
```
