# Active Memory

Approved durable project memory lives here.

Rules:

- Do not store secrets, credentials, API keys, tokens, or PII.
- Keep each memory record focused on one durable idea.
- Every record should have a source stamp.
- Update `.agent-loop/MEMORY.md` when a record is approved.

