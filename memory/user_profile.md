---
name: User Profile
description: Who the user is, their experience level, working style, and collaboration preferences on this project
type: user
---

# User Profile

- Experienced developer — built production projects (e.g. FilmGap: Go backend, PostgreSQL, server-rendered HTML, full auth/moderation/SEO system)
- Heavy Claude Code user — thinks meta about AI-assisted development, not just using it
- Iterative thinker — prefers decide → research → document → re-grill → build → test cycles over big-bang planning
- Evidence-driven — wants decisions backed by research, not just intuition
- Pragmatic about scope — defers non-critical features (e.g. CLI) to v2 without being asked twice
- Wants to build things, not just plan them — dislikes endless questions without producing artifacts
- Aware of LLM limitations — proactively manages context window, memory, and compaction risk
- Attentive to detail — catches structural mistakes (e.g. wrong memory file location) before moving on
